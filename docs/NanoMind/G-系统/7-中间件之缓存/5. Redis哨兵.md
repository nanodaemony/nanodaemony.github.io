#### 普通的主从复制有什么问题？

- 出现故障需要**人工解决**。
- 主节点读写能力有限，容易遇到**单点问题**。不够高可用。

#### 哨兵的部署架构？

Sentinel 是一个分布式架构，其中包含**若干个 Sentinel 节点和 Redis 数据节点**。主节点出现故障时，Sentinel 能**自动完成故障发现和故障转移，整个过程完全是自动的，不需要人工来介入**，从而实现真正的高可用。

**每个 Sentinel 节点会对数据节点和其余 Sentinel 节点进行监控，当它发现节点不可达时，会对节点做下线标识。如果下线的是主节点，当大多数 Sentinel 节点都认为主节点不可达时，它们会选举出一个 Sentinel 节点来完成自动故障转移，同时会将这个变化实时通知给客户端**。

Redis Sentinel 与普通的主从复制的架构相比较只是多了若干 Sentinel 节点，所以 Redis Sentinel 并没有针对 Redis 节点做特殊处理。

注意：**在 Sentinel 架构中，通常不会存在多个主节点**。Sentinel 的核心设计目标是**监控和管理一个主从复制集群，确保主节点故障时能自动完成故障转移(Failover)，并始终维持唯一的主节点**。因此下图中，**这个 Sentinel 集群可以同时监控\*\***多个独立的主从复制集群\***\*(即多个主节点及其从节点)，多个主节点之间存放的是不同的数据。**

![](/zimg/yuque-1778165494626-1.png)

Sentinel 哨兵是特殊的 Redis 服务，不提供读写服务，主要用来监控 Redis 实例节点。哨兵节点本身就是独立的 Redis 节点，只不过它们有一些特殊，它们不存储数据，只支持部分命令。

#### 为什么需要多个哨兵？

- **防止误判**。节点故障判断由多个 Sentinel 节点共同完成，可以防止误判。
- **哨兵自身高可用**。防止 Sentinel 自身出现单点故障。

#### 哨兵有什么作用？

**哨兵的主要作用是为 Redis 主从架构提供高可用性支持**。

哨兵主要负责的就是三个作用：**监控，选主(选择主库)和通知**。

1. **监控**。监控是指哨兵进程在运行时，周期性地给所有的主从库发送 `PING` 命令，检测它们是否仍然在线运行。如果从库没有在规定时间内响应哨兵的 `PING` 命令，哨兵就会把它标记为 "下线状态"；同样如果主库也没有在规定时间内响应哨兵的 `PING` 命令，哨兵就会判定主库下线，然后开始自动切换主库的流程。
2. **选择主库**。哨兵可以自动切换主库(也可以认为是自动的故障转移)。主库挂了以后，哨兵就需要从**多个从库里，按照一定的规则选择一个从库实例，把它作为新的主库**。这一步完成后，现在的集群里就有了新主库。
3. **通知**。在执行通知任务时，哨兵会**把新主库的连接信息发给其他从库，让它们执行** `**replicaof**` **命令，和新主库建立连接，并进行数据复制**。同时哨兵会把新主库的连接信息**通知给客户端**，让它们把请求操作发到新主库上。

下图展示了哨兵的作用和它们各自的目标。

![](/zimg/yuque-1778165494854-2.png)

在这三个任务中，通知任务相对来说比较简单，哨兵只需要把新主库信息发给从库和客户端，让它们和新主库建立连接就行，并不涉及决策的逻辑。但在监控和选主这两个任务中，**哨兵需要做出两个决策**：

1. 在监控任务中，哨兵需要判断主库是否处于下线状态。
2. 在选主任务中，哨兵也要决定选择哪个从库实例作为主库。

#### 哨兵是如何监控其他结点的？

通过**三个定时监控任务**完成对各个节点发现和监控。

- **Sentinel 与数据结点的定时任务**。每隔 10 秒，每个 Sentinel 节点会向主节点和从节点发送 `info` 命令获取最新的拓扑结构。
- **Sentinel 之间的定时任务**。每隔 2 秒，每个 Sentinel 节点会向 Redis 数据节点的 `sentinel:hello` 频道上发送该 Sentinel 节点对于主节点的判断以及当前 Sentinel 节点的信息。
- **心跳定时任务**。每隔 1 秒，每个 Sentinel 节点会向主节点，从节点，其余 Sentinel 节点发送一条 `ping` 命令做一次心跳检测，来确认这些节点当前是否可达。

#### 哨兵如何判断其他节点是否下线？

**主观下线**：哨兵监控基于**心跳监控**其他节点的时候，如果节点超过 `down-after-milliseconds` 时间没有进行有效回复，这个 Sentinel 节点就会对该节点做失败判定，这个行为叫做**主观下线**。从字面意思也可以看出**主观下线是当前 Sentinel 节点的一家之言，存在误判的可能**。也就是可能就这一个 Sentinel 认为一个结点是下线了，但其实并没有下线。误判一般会发生在集群网络压力较大，网络拥塞，或者是主库本身压力较大的情况下。

**客观下线**：在哨兵集群下。引入多个哨兵实例一起来判断，就可以**避免单个哨兵因为自身网络状况不好，而误判主库下线的情况**。同时多个哨兵的网络同时不稳定的概率较小，由它们一起做决策，误判率也能降低。当 Sentinel 主观下线的节点是主节点时，该 Sentinel 节点会通过 `sentinel ismaster-down-by-addr` 命令向其他 Sentinel 节点询问对主节点的判断，**当超过** `**quorum**` **个主观下线的回复时，Sentinel 节点认为主节点确实有问题，这时该 Sentinel 节点会做出客观下线的判定，也就是大部分 Sentinel 节点都对主节点的下线做了同意的判定，那么这个判定就是客观的，就叫客观下线**。

#### 哨兵如何进行自动故障转移？如果一个主节点客观下线了怎么办？

如果哨兵集群认定一个主节点客观下线了，那就会**重新选一个新的主节点**。

流程如下：

- 首先**哨兵集群之间会基于 Raft 算法选出一个哨兵结点来领导后续的故障转移工作**。
- **从主节点的从节点选出一个节点作为新的主节点**，选择流程如下：哨兵会按照在线状态，网络状态，筛选过滤掉一部分不符合要求的从库，然后依次按照优先级，复制进度，ID 号大小再对剩余的从库进行打分，只要有得分最高的从库出现，就把它选为新主库。

#### 需要部署多少个哨兵结点？

**部署三个以上且奇数个的 Sentinel 节点**。3 个以上是通过增加 Sentinel 节点的个数提高对于故障判定的准确性，因为领导者选举需要至少一半加 1 个节点，奇数个节点可以在满足该条件的基础上节省一个节点。
